// Function to print all three-digit numbers in decreasing order
function printThreeDigitNumbers() {
    for (let num = 999; num >= 100; num--) {
        console.log(num);
    }
}

// Call the function to print the numbers
printThreeDigitNumbers();

console.log(printThreeDigitNumbers)